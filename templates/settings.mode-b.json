{
  "_comment": "Mode B settings: declarative deny rules + dynamic guard hook. Layer 1 (deny rules) cannot be overridden by a hook allow; layer 2 (PreToolUse hook) blocks even in bypassPermissions. Verify at wiring time that CLAUDE_PROJECT_DIR expands in your Claude Code version; hook $VARs not allowlisted expand to empty string.",
  "permissions": {
    "deny": [
      "Bash(rm:*)", "Bash(rmdir:*)", "Bash(sudo:*)",
      "Bash(git push --force:*)", "Bash(git reset --hard:*)",
      "Bash(git clean:*)", "Bash(git add -A:*)", "Bash(git add --all:*)",
      "WebFetch", "WebSearch"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/.harness/pack/scripts/guard_pretooluse.py\""
          }
        ]
      }
    ]
  }
}
